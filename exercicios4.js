// Capturar dois valores (dois inputs)
// somar e exibir na tela;

function somaValores() {
    let primeiroValor = +valor.value;
    let segundoValor = +valor2.value;
    let resultado = primeiroValor + segundoValor;

    alert(resultado)
}